# application.yml 예시
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/batch_test
    username: root
    password: New1234!
  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate / none
    show-sql: true
  profiles:
    active: dev          # dev / prod 환경 분리